apiVersion: v1
kind: Service
metadata:
  name: model-webhook
  namespace: matrixinfer-system
  labels:
    app: model-webhook
spec:
  ports:
  - port: 443
    targetPort: 8443
    name: https
  selector:
    app: model-webhook