apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: matrixinfer-system

resources:
- cert-manager.yaml
- deployment.yaml
- service.yaml
- validating-webhook.yaml
- mutating-webhook.yaml

# Replace the image placeholder with the actual image
images:
- name: registry-webhook
  newName: ghcr.io/matrixinfer-ai/registry-webhook
  newTag: latest
