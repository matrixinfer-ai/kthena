apiVersion: v1
kind: Service
metadata:
  name: registry-webhook
  namespace: matrixinfer-system
  labels:
    app: registry-webhook
spec:
  ports:
  - port: 443
    targetPort: 8443
    name: https
  selector:
    app: registry-webhook