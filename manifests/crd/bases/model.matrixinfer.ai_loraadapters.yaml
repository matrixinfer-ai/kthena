---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: loraadapters.model.matrixinfer.ai
spec:
  group: model.matrixinfer.ai
  names:
    kind: LoraAdapter
    listKind: LoraAdapterList
    plural: loraadapters
    singular: loraadapter
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: LoraAdapter is the Schema for the loraadapters API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: LoraAdapterSpec defines the desired state of LoraAdapter.
            properties:
              modelURI:
                description: ModelURI is the URI of the lora adapter.
                maxLength: 256
                type: string
              name:
                description: Name is the name of the lora adapter.
                maxLength: 128
                type: string
              owner:
                description: Owner is the owner of the lora adapter.
                maxLength: 128
                type: string
            required:
            - modelURI
            type: object
          status:
            description: LoraAdapterStatus defines the observed state of LoraAdapter.
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
