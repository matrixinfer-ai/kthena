---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: autoscalingpolicies.registry.matrixinfer.ai
spec:
  group: registry.matrixinfer.ai
  names:
    kind: AutoscalingPolicy
    listKind: AutoscalingPolicyList
    plural: autoscalingpolicies
    singular: autoscalingpolicy
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AutoscalingPolicy is the Schema for the autoscalingpolicies API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AutoscalingPolicySpec defines the desired state of AutoscalingPolicy.
            properties:
              behavior:
                description: Behavior defines the scaling behavior for both scale
                  up and scale down.
                properties:
                  scaleDown:
                    description: ScaleDown defines the policy for scaling down (decreasing
                      replicas).
                    properties:
                      panicModeHold:
                        description: PanicModeHold is the duration to hold in panic
                          mode before returning to normal.
                        type: string
                      panicThresholdPercent:
                        description: PanicThresholdPercent is the threshold percent
                          to enter panic mode.
                        format: int32
                        maximum: 100
                        minimum: 0
                        type: integer
                      percent:
                        description: Percent is the percentage of instances to remove
                          during scaling down.
                        format: int32
                        maximum: 100
                        minimum: 0
                        type: integer
                      period:
                        description: Period is the duration over which scaling down
                          is evaluated.
                        type: string
                    required:
                    - panicModeHold
                    - panicThresholdPercent
                    - percent
                    - period
                    type: object
                  stable:
                    description: ScaleUp defines the policy for scaling up (increasing
                      replicas).
                    properties:
                      instances:
                        description: Instances is the minimum number of instances
                          to scale up.
                        format: int32
                        minimum: 0
                        type: integer
                      percent:
                        description: Percent is the percentage of instances to add
                          during scaling up.
                        format: int32
                        maximum: 100
                        minimum: 0
                        type: integer
                      period:
                        description: Period is the duration over which scaling up
                          is evaluated.
                        type: string
                      selectPolicy:
                        description: SelectPolicy determines the selection strategy
                          for scaling up (e.g., max, min).
                        type: string
                      stabilizationWindow:
                        description: StabilizationWindow is the time window to stabilize
                          scaling up actions.
                        type: string
                    required:
                    - instances
                    - percent
                    - period
                    - selectPolicy
                    - stabilizationWindow
                    type: object
                required:
                - scaleDown
                - stable
                type: object
              metrics:
                description: Metrics is the list of metrics used to evaluate scaling
                  decisions.
                items:
                  description: AutoscalingPolicyMetric defines a metric and its target
                    value for scaling.
                  properties:
                    metricName:
                      description: MetricName is the name of the metric to monitor.
                      type: string
                    targetValue:
                      description: TargetValue is the target value for the metric
                        to trigger scaling.
                      format: int32
                      type: integer
                  required:
                  - metricName
                  - targetValue
                  type: object
                minItems: 1
                type: array
              tolerancePercent:
                description: TolerancePercent is the percentage of deviation tolerated
                  before scaling actions are triggered.
                format: int32
                maximum: 100
                minimum: 0
                type: integer
            required:
            - behavior
            - metrics
            - tolerancePercent
            type: object
          status:
            description: AutoscalingPolicyStatus defines the observed state of AutoscalingPolicy.
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
