apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "matrixinfer.fullname" . }}-infer-gateway
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/component: infer-gateway
    {{- include "matrixinfer.labels" . | nindent 4 }}
