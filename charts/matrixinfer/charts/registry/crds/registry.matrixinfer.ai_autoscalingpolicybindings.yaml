---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: autoscalingpolicybindings.registry.matrixinfer.ai
spec:
  group: registry.matrixinfer.ai
  names:
    kind: AutoscalingPolicyBinding
    listKind: AutoscalingPolicyBindingList
    plural: autoscalingpolicybindings
    singular: autoscalingpolicybinding
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AutoscalingPolicyBinding is the Schema for the models API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AutoscalingPolicyBindingSpec defines the desired state of
              AutoscalingPolicyBinding.
            properties:
              autoscalingPolicyRef:
                description: PolicyRef references the autoscaling policy to be optimized
                  scaling base on multiple targets.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              optimizerConfiguration:
                properties:
                  costExpansionRatePercent:
                    description: CostExpansionRatePercent is the percentage rate at
                      which the cost expands.
                    format: int32
                    maximum: 100
                    minimum: 0
                    type: integer
                  targets:
                    items:
                      properties:
                        cost:
                          description: Cost is the cost associated with running this
                            backend.
                          format: int32
                          minimum: 0
                          type: integer
                        maxReplicas:
                          description: MaxReplicas is the maximum number of replicas
                            for the backend.
                          format: int32
                          maximum: 1000000
                          minimum: 1
                          type: integer
                        minReplicas:
                          description: MinReplicas is the minimum number of replicas
                            for the backend.
                          format: int32
                          maximum: 1000000
                          minimum: 0
                          type: integer
                        target:
                          properties:
                            kind:
                              type: string
                            metricFrom:
                              properties:
                                matchLabels:
                                  additionalProperties:
                                    type: string
                                  type: object
                                port:
                                  default: "8100"
                                  format: int32
                                  type: integer
                                url:
                                  default: /metrics
                                  description: The metric uri, e.g. /metrics
                                  type: string
                              type: object
                            name:
                              type: string
                            targetRef:
                              description: |-
                                LocalObjectReference contains enough information to let you locate the
                                referenced object inside the same namespace.
                              properties:
                                name:
                                  default: ""
                                  description: |-
                                    Name of the referent.
                                    This field is effectively required, but due to backwards compatibility is
                                    allowed to be empty. Instances of this type with an empty value here are
                                    almost certainly wrong.
                                    More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                  type: string
                              type: object
                              x-kubernetes-map-type: atomic
                          required:
                          - kind
                          - name
                          type: object
                      required:
                      - maxReplicas
                      - minReplicas
                      type: object
                    type: array
                type: object
              scalingConfiguration:
                properties:
                  maxReplicas:
                    description: MaxReplicas is the maximum number of replicas for
                      the backend.
                    format: int32
                    maximum: 1000000
                    minimum: 1
                    type: integer
                  minReplicas:
                    description: MinReplicas is the minimum number of replicas for
                      the backend.
                    format: int32
                    maximum: 1000000
                    minimum: 0
                    type: integer
                  target:
                    properties:
                      kind:
                        type: string
                      metricFrom:
                        properties:
                          matchLabels:
                            additionalProperties:
                              type: string
                            type: object
                          port:
                            default: "8100"
                            format: int32
                            type: integer
                          url:
                            default: /metrics
                            description: The metric uri, e.g. /metrics
                            type: string
                        type: object
                      name:
                        type: string
                      targetRef:
                        description: |-
                          LocalObjectReference contains enough information to let you locate the
                          referenced object inside the same namespace.
                        properties:
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                        type: object
                        x-kubernetes-map-type: atomic
                    required:
                    - kind
                    - name
                    type: object
                required:
                - maxReplicas
                - minReplicas
                type: object
            type: object
          status:
            description: AutoscalingPolicyBindingStatus defines the status of a autoscaling
              policy binding.
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
