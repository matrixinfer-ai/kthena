apiVersion: v1
kind: Secret
metadata:
  name: downloader-secrets
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/component: downloader-secrets
    {{- include "kthena.labels" . | nindent 4 }}
data:
  ACCESS_KEY: {{ .Values.modelBoosterController.downloader.accessKey | b64enc | quote }}
  SECRET_KEY: {{ .Values.modelBoosterController.downloader.secretKey | b64enc | quote }}
type: Opaque
  