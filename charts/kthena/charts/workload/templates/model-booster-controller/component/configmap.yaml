apiVersion: v1
kind: ConfigMap
metadata:
  name: model-booster-controller-config
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/component: model-booster-controller
    {{- include "kthena.labels" . | nindent 4 }}
data:
  model_serving_downloader_image: "{{ .Values.modelBoosterController.downloaderImage.repository }}:{{ .Values.modelBoosterController.downloaderImage.tag }}"
  model_serving_runtime_image: "{{ .Values.modelBoosterController.runtimeImage.repository }}:{{ .Values.modelBoosterController.runtimeImage.tag }}"
