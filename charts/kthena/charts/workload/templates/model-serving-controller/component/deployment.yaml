apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "kthena.name" . }}-model-serving-controller
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/component: model-serving-controller
    {{- include "kthena.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.modelServingController.replicas }}
  selector:
    matchLabels:
      app.kubernetes.io/component: model-serving-controller
      {{- include "kthena.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        app.kubernetes.io/component: model-serving-controller
        {{- include "kthena.labels" . | nindent 8 }}
    spec:
      containers:
        - name: model-serving-controller
          image: "{{ .Values.modelServingController.image.repository }}:{{ .Values.modelServingController.image.tag }}"
          imagePullPolicy: {{ .Values.modelServingController.image.pullPolicy }}
          args:
            {{- toYaml .Values.modelServingController.image.args | nindent 12 }}
          resources: {{- toYaml .Values.modelServingController.resource | nindent 12 }}
      serviceAccountName: {{ include "kthena.name" . }}-model-serving-controller
