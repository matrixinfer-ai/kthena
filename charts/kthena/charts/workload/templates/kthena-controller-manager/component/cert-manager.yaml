{{- if and .Values.controllerManager.webhook.enabled  .Values.global.certManager.enabled }}
apiVersion: cert-manager.io/v1
kind: Issuer
metadata:
  name: kthena-webhook-issuer
  namespace: {{ .Release.Namespace }}
spec:
  selfSigned: {}
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: kthena-webhook-cert
  namespace: {{ .Release.Namespace }}
spec:
  dnsNames:
    - kthena-webhook.{{ .Release.Namespace }}.svc
    - kthena-webhook.{{ .Release.Namespace }}.svc.cluster.local
  issuerRef:
    kind: Issuer
    name: kthena-webhook-issuer
  secretName: {{ .Values.controllerManager.webhook.tls.certSecretName }}
{{- end }}
