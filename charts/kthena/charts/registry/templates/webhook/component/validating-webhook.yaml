{{- if .Values.webhook.enabled }}
apiVersion: admissionregistration.k8s.io/v1
kind: ValidatingWebhookConfiguration
metadata:
  name: {{ include "kthena.name" . }}-validating-webhook
  labels:
    app.kubernetes.io/component: webhook
    {{- include "kthena.labels" . | nindent 4 }}
  {{- if .Values.global.certManager.enabled }}
  annotations:
    cert-manager.io/inject-ca-from: {{ .Release.Namespace }}/{{ include "kthena.name" . }}-webhook-cert
  {{- end }}
webhooks:
  - name: validate-registry-kthena-ai-v1alpha1-model.kthena.ai
    admissionReviewVersions: [ "v1" ]
    sideEffects: None
    failurePolicy: Fail
    timeoutSeconds: 30
    clientConfig:
      service:
        name: {{ include "kthena.name" . }}-webhook
        namespace: {{ .Release.Namespace }}
        path: "/validate-registry-kthena-ai-v1alpha1-model"
        port: 443
      {{- if .Values.global.certManager.enabled }}
      caBundle: ""
      {{- else }}
      caBundle: {{ required "A caBundle is required for the registry validating webhook when cert-manager is disabled (global.webhook.caBundle)" .Values.global.webhook.caBundle | quote }}
      {{- end }}
    rules:
      - apiGroups: [ "registry.kthena.ai" ]
        apiVersions: [ "v1alpha1" ]
        resources: [ "models" ]
        operations: [ "CREATE", "UPDATE" ]
        scope: "Namespaced"
  - name: validate-registry-kthena-ai-v1alpha1-autoscalingpolicy.volcano.sh
    admissionReviewVersions: [ "v1" ]
    sideEffects: None
    failurePolicy: Fail
    timeoutSeconds: 30
    clientConfig:
      service:
        name: {{ include "kthena.name" . }}-webhook
        namespace: {{ .Release.Namespace }}
        path: "/validate-registry-volcano-sh-v1alpha1-autoscalingpolicy"
        port: 443
      {{- if .Values.global.certManager.enabled }}
      caBundle: ""
      {{- else }}
      caBundle: {{ required "A caBundle is required for the registry validating webhook when cert-manager is disabled (global.webhook.caBundle)" .Values.global.webhook.caBundle | quote }}
      {{- end }}
    rules:
      - apiGroups: [ "registry.volcano.sh" ]
        apiVersions: [ "v1alpha1" ]
        resources: [ "autoscalingpolicies" ]
        operations: [ "CREATE", "UPDATE" ]
        scope: "Namespaced"
  - name: validate-registry-volcano-sh-v1alpha1-autoscalingpolicybinding.volcano.sh
    admissionReviewVersions: [ "v1" ]
    sideEffects: None
    failurePolicy: Fail
    timeoutSeconds: 30
    clientConfig:
      service:
        name: {{ include "kthena.name" . }}-webhook
        namespace: {{ .Release.Namespace }}
        path: "/validate-registry-volcano-sh-v1alpha1-autoscalingpolicybinding"
        port: 443
      {{- if .Values.global.certManager.enabled }}
      caBundle: ""
      {{- else }}
      caBundle: {{ required "A caBundle is required for the registry validating webhook when cert-manager is disabled (global.webhook.caBundle)" .Values.global.webhook.caBundle | quote }}
      {{- end }}
    rules:
      - apiGroups: [ "registry.kthena.ai" ]
        apiVersions: [ "v1alpha1" ]
        resources: [ "autoscalingpolicybindings" ]
        operations: [ "CREATE", "UPDATE" ]
        scope: "Namespaced"
{{- end }}
